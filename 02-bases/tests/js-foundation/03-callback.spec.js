const { getUserById } = require('../../src/js-foundation/03-callbacks')

describe('03-callback.ts', () => {
  test('getUserById should return an error if user not exist', () => {
    const id =10;
    getUserById(id, (err, user) => )
  })
})